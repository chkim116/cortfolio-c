import styled from "@emotion/styled";

const Container = styled.footer`
    width: 100%;
    text-align: center;
    min-height: 12vh;
`;

const Footer = () => {
    return <Container>&copy; Cortfolio</Container>;
};

export default Footer;

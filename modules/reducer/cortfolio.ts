import { createSlice } from "@reduxjs/toolkit";

const cortfolio = createSlice({
    name: "cortfolio",
    initialState: {},
    reducers: {},
});

export const {} = cortfolio.actions;

export default cortfolio.reducer;
